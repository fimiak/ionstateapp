extends layout

block content
  h3= title

  ul(class='list')
    each summit in summit_list
      li
        a(href='google.com') #{summit.name}
        |  -- (#{summit.date_formatted})

    else
      li There are no summits.